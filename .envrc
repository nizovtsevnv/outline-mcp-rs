# Автоматически входим в Nix flake окружение при входе в директорию
# Требует установки direnv: https://direnv.net/
use flake

# Проверяем, что используется Rust 2024 edition
echo "🦀 Загружено окружение Rust 2024 edition"
echo "📦 Rust version: $(rustc --version 2>/dev/null || echo 'Не загружен')"

# Дополнительные переменные для разработки
export RUST_BACKTRACE=1
export RUST_LOG=debug 